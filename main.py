#!/usr/bin/env python3
"""
Numerical Integration Framework — main entry point.

Run the interactive CLI to compute definite integrals with LaTeX input,
multiple quadrature methods, convergence analysis, and visualization.
"""

from src.cli import run_cli

if __name__ == "__main__":
    run_cli()
